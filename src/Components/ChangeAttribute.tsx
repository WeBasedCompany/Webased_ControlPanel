import React from 'react'

const ChangeAttribute = ({api, command, ...others}: {api: string, command: string}) => {
    return (
        <div>
            
        </div>
    )
}

export default ChangeAttribute
